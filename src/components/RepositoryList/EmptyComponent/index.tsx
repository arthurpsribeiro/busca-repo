import React from 'react';

import { Container, Title } from './styles';

const EmptyComponent: React.FC = () => {
  return (
    <Container>
      <Title>Faça uma busca para exibir os repositórios!</Title>
    </Container>
  );
};

export default EmptyComponent;
